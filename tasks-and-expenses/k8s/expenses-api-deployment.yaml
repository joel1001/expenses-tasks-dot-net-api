apiVersion: apps/v1
kind: Deployment
metadata:
  name: expenses-api
  labels:
    app: expenses-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: expenses-api
  template:
    metadata:
      labels:
        app: expenses-api
    spec:
      containers:
      - name: expenses-api
        image: expenses-api:latest
        ports:
        - containerPort: 8080
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Development"
        - name: ConnectionStrings__DefaultConnection
          valueFrom:
            configMapKeyRef:
              name: expenses-config
              key: connection-string
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: expenses-api
spec:
  selector:
    app: expenses-api
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
  type: ClusterIP
